class Award {
  public nombre: string;
  public descripcion: string;
  public anio: string;

  constructor(nombre: string, descripcion: string, anio: string) {
    this.nombre = nombre;
    this.descripcion = descripcion;
    this.anio = anio;
  }
}

export default Award;
